{
  "name": "get-obj-hash",
  "version": "1.0.0",
  "description": "Get the hash of a object or primitive using BLAKE2b or BLAKE2s",
  "repository": "https://github.com/tiaanduplessis/get-obj-hash",
  "author": "Tiaan du Plesssis <tiaanduplessis@hotmail.com>",
  "license": "MIT",
  "main": "dist/get-obj-hash.js",
  "umd:main": "dist/get-obj-hash.umd.js",
  "module": "dist/get-obj-hash.m.js",
  "source": "src/index.js",
  "scripts": {
    "lint": "standard --fix ./src/**/*.js",
    "test": "jest",
    "build": "microbundle -o dist/ --sourcemap false --compress false",
    "dev": "microbundle watch -o dist/ --sourcemap false --compress false"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.3.1",
    "babel-eslint": "^10.0.1",
    "jest": "^24.1.0",
    "microbundle": "^0.9.0",
    "standard": "^12.0.1"
  },
  "dependencies": {
    "blakejs": "^1.1.0",
    "fast-stringify": "^1.1.1",
    "nova-is-obj": "^1.0.0"
  },
  "files": [
    "dist"
  ],
  "standard": {
    "parser": "babel-eslint",
    "env": {
      "jest": true
    }
  }
}
